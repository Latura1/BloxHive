<UserControl x:Class="BloxHive.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BloxHive.ViewModels"
             d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
             mc:Ignorable="d">
    <ScrollViewer Padding="40" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="640">
            <TextBlock Text="{Binding Loc.SettingsTitle}" Style="{StaticResource PageTitle}" />
            <TextBlock Text="{Binding Loc.SettingsDescription}" Style="{StaticResource PageSubtitle}" />

            <Border Style="{StaticResource Card}" Margin="0 0 0 16">
                <StackPanel>
                    <TextBlock Text="{Binding Loc.Language}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                    <TextBlock Text="{Binding Loc.LanguageDescription}" FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Margin="0 4 0 20" />
                    <ComboBox ItemsSource="{Binding Languages}" SelectedIndex="{Binding SelectedLanguageIndex}" Width="220" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0 0 0 16">
                <StackPanel>
                    <TextBlock Text="{Binding Loc.Theme}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                    <TextBlock Text="{Binding Loc.ThemeDescription}" FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Margin="0 4 0 20" />
                    <WrapPanel Margin="0 0 0 12">
                        <Button Style="{StaticResource ThemeSwatch}" Background="#F59E0B" Command="{Binding SetThemeCommand}" CommandParameter="0" />
                        <Button Style="{StaticResource ThemeSwatch}" Background="#3B82F6" Command="{Binding SetThemeCommand}" CommandParameter="1" Margin="16 0 0 0" />
                        <Button Style="{StaticResource ThemeSwatch}" Background="#8B5CF6" Command="{Binding SetThemeCommand}" CommandParameter="2" Margin="16 0 0 0" />
                        <Button Style="{StaticResource ThemeSwatch}" Background="#22C55E" Command="{Binding SetThemeCommand}" CommandParameter="3" Margin="16 0 0 0" />
                        <Button Style="{StaticResource ThemeSwatch}" Background="#EF4444" Command="{Binding SetThemeCommand}" CommandParameter="4" Margin="16 0 0 0" />
                        <Button Style="{StaticResource ThemeSwatch}" Background="#06B6D4" Command="{Binding SetThemeCommand}" CommandParameter="5" Margin="16 0 0 0" />
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="{Binding Loc.ThemeDefault}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Width="72" TextAlignment="Center" />
                        <TextBlock Text="{Binding Loc.ThemeBlue}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Width="72" TextAlignment="Center" Margin="16 0 0 0" />
                        <TextBlock Text="{Binding Loc.ThemePurple}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Width="72" TextAlignment="Center" Margin="16 0 0 0" />
                        <TextBlock Text="{Binding Loc.ThemeForest}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Width="72" TextAlignment="Center" Margin="16 0 0 0" />
                        <TextBlock Text="{Binding Loc.ThemeRuby}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Width="72" TextAlignment="Center" Margin="16 0 0 0" />
                        <TextBlock Text="{Binding Loc.ThemeCyber}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Width="72" TextAlignment="Center" Margin="16 0 0 0" />
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="{Binding Loc.Webhook}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                    <TextBlock Text="{Binding Loc.WebhookDescription}" FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Margin="0 4 0 20" />

                    <TextBlock Text="{Binding Loc.WebhookUrl}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Margin="0 0 0 6" />
                    <TextBox Style="{StaticResource SettingsInput}" Text="{Binding WebhookUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="{Binding Loc.WebhookUrlPlaceholder}" FontSize="11" Foreground="{DynamicResource BrushTextMuted}" Margin="0 4 0 20" />

                    <StackPanel Orientation="Horizontal" Margin="0 0 0 4">
                        <TextBlock Text="{Binding Loc.WebhookInterval}" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding IntervalValue, StringFormat='{}{0}s'}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource BrushAccent}" Margin="12 0 0 0" VerticalAlignment="Center" />
                    </StackPanel>
                    <Slider Minimum="5" Maximum="600" TickFrequency="5" IsSnapToTickEnabled="True"
                            Value="{Binding IntervalValue, Mode=TwoWay}"
                            Foreground="{DynamicResource BrushAccent}" Margin="0 4 0 4" />
                    <TextBlock Text="{Binding Loc.WebhookIntervalDescription}" FontSize="11" Foreground="{DynamicResource BrushTextMuted}" Margin="0 4 0 20" />

                    <Button Height="36" Padding="16 0" Background="{DynamicResource BrushAccent}" Foreground="White" BorderThickness="0" Cursor="Hand" Command="{Binding TestWebhookCommand}" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0 0 8 0" />
                            <TextBlock Text="{Binding Loc.TestWebhook}" FontSize="13" FontWeight="SemiBold" />
                        </StackPanel>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{DynamicResource BrushAccent}" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="8" Padding="{TemplateBinding Padding}" Height="{TemplateBinding Height}" Cursor="Hand">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.85" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock Text="{Binding WebhookStatus}" FontSize="13" Foreground="{DynamicResource BrushAccent}" Margin="0 8 0 0" Visibility="{Binding WebhookStatus, Converter={StaticResource StringToVisibility}}" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Loc.Experimental}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                            <TextBlock Text="{Binding Loc.ExperimentalDescription}" FontSize="12" Foreground="{DynamicResource BrushTextMuted}" Margin="0 4 0 0" />
                        </StackPanel>
                        <ToggleButton Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ExperimentalFeatures, Mode=TwoWay}" VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
