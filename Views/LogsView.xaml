<UserControl x:Class="BloxHive.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BloxHive.ViewModels"
             xmlns:m="clr-namespace:BloxHive.Models"
             d:DataContext="{d:DesignInstance vm:LogsViewModel}"
             mc:Ignorable="d">
    <ScrollViewer Padding="40" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="720">
            <TextBlock Text="{Binding Loc.LogsTitle}" Style="{StaticResource PageTitle}" />
            <TextBlock Text="{Binding Loc.LogsDescription}" Style="{StaticResource PageSubtitle}" />

            <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
                <Border CornerRadius="8" Padding="4" Background="{DynamicResource BrushSurfaceDarkTertiary}">
                    <StackPanel Orientation="Horizontal">
                        <ToggleButton IsChecked="{Binding FilterInfo}" Command="{Binding FilterCommand}"
                                      Height="30" Padding="12 0" Cursor="Hand" FontSize="12" FontWeight="SemiBold"
                                      Foreground="{DynamicResource BrushTextSecondary}" Background="Transparent" BorderThickness="0"
                                      Content="INFO">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}" Height="{TemplateBinding Height}" Cursor="Hand">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="{DynamicResource BrushAccent}" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ToggleButton IsChecked="{Binding FilterWarning}" Command="{Binding FilterCommand}"
                                      Height="30" Padding="12 0" Cursor="Hand" FontSize="12" FontWeight="SemiBold"
                                      Foreground="{DynamicResource BrushTextSecondary}" Background="Transparent" BorderThickness="0"
                                      Content="WARN" Margin="4 0 0 0">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}" Height="{TemplateBinding Height}" Cursor="Hand">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#F59E0B" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ToggleButton IsChecked="{Binding FilterError}" Command="{Binding FilterCommand}"
                                      Height="30" Padding="12 0" Cursor="Hand" FontSize="12" FontWeight="SemiBold"
                                      Foreground="{DynamicResource BrushTextSecondary}" Background="Transparent" BorderThickness="0"
                                      Content="ERROR" Margin="4 0 0 0">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}" Height="{TemplateBinding Height}" Cursor="Hand">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#EF4444" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </StackPanel>
                </Border>

                <Button Command="{Binding ClearCommand}" Margin="12 0 0 0"
                        Height="30" Padding="12 0" Background="Transparent"
                        Foreground="{DynamicResource BrushTextSecondary}"
                        BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Cursor="Hand"
                        FontSize="12" Content="{Binding Loc.LogsClear}" />
                <Button Command="{Binding CopyFilteredCommand}" Margin="8 0 0 0"
                        Height="30" Padding="12 0" Background="Transparent"
                        Foreground="{DynamicResource BrushTextSecondary}"
                        BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Cursor="Hand"
                        FontSize="12" Content="{Binding Loc.Copy}" />
                <Button Command="{Binding DownloadLogsCommand}" Margin="8 0 0 0"
                        Height="30" Padding="12 0" Background="Transparent"
                        Foreground="{DynamicResource BrushTextSecondary}"
                        BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Cursor="Hand"
                        FontSize="12" Content="{Binding Loc.LogsDownload}" />
            </StackPanel>

            <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{DynamicResource BrushAccent}" Margin="0 0 0 8" 
                       Visibility="{Binding StatusText, Converter={StaticResource StringToVisibility}}" />

            <Border Style="{StaticResource Card}" Padding="0">
                <ListBox ItemsSource="{Binding FilteredEntries}" Background="Transparent"
                         BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         FontFamily="Cascadia Code, Consolas, monospace" FontSize="12">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type m:LogEntry}">
                            <Border Padding="12 8" Background="Transparent">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss'}" Foreground="{DynamicResource BrushTextMuted}" Width="60" />
                                    <TextBlock Text="{Binding LevelLabel}" Width="50"
                                               Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Source}" Foreground="{DynamicResource BrushAccent}" Width="100" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Message}" Foreground="{DynamicResource BrushTextPrimary}" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="bg" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
