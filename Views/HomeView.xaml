<UserControl x:Class="BloxHive.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BloxHive.ViewModels"
             d:DataContext="{d:DesignInstance vm:HomeViewModel}"
             mc:Ignorable="d">
    <ScrollViewer Padding="40" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 32">
                <Border Width="48" Height="48" CornerRadius="14" Background="{DynamicResource BrushAccent}" VerticalAlignment="Center">
                    <TextBlock Text="B" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel VerticalAlignment="Center" Margin="16 0 0 0">
                    <TextBlock Text="{Binding Loc.HomeWelcome}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                    <TextBlock Text="{Binding Loc.HomeHint}" FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Margin="0 4 0 0" />
                </StackPanel>
            </StackPanel>

            <Border Style="{StaticResource Card}" Margin="0 0 0 16">
                <StackPanel>
                    <TextBlock Text="{Binding Loc.MultiInstance}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                    <TextBlock Text="{Binding Loc.MultiInstanceDescription}" FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Margin="0 4 0 20" />

                    <Border Background="{DynamicResource BrushSurfaceDarkTertiary}" CornerRadius="12" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Loc.MultiInstanceToggle}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                                <TextBlock Text="{Binding MultiInstanceStatus}" FontSize="12" Foreground="{DynamicResource BrushAccent}" Margin="0 4 0 0" />
                            </StackPanel>

                            <ToggleButton Grid.Column="2" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding IsMultiInstanceActive, Mode=TwoWay}" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <Grid Margin="0 0 0 16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ProcessCountText}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                            <TextBlock Text="&#xE72D;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource BrushTextMuted}" VerticalAlignment="Center" Margin="0 0 6 0" />
                            <TextBlock Text="Loop" FontSize="12" Foreground="{DynamicResource BrushTextMuted}" VerticalAlignment="Center" Margin="0 0 8 0" />
                            <ToggleButton Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AutoLoopActive, Mode=TwoWay}" VerticalAlignment="Center" />
                        </StackPanel>

                        <Button Grid.Column="3" Style="{StaticResource DangerButton}" Command="{Binding KillAllCommand}" Visibility="{Binding HasProcesses, Converter={StaticResource BoolToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <Viewbox Width="14" Height="14" Margin="0 0 6 0" VerticalAlignment="Center">
                                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" />
                                </Viewbox>
                                <TextBlock Text="{Binding Loc.CloseAll}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding Processes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource BrushSurfaceDarkTertiary}" CornerRadius="10" Padding="10 8" Margin="0 0 0 6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="6" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <ToggleButton Grid.Column="0" IsChecked="{Binding IsWebhookEnabled}" Width="20" Height="20" Cursor="Hand" VerticalAlignment="Center">
                                            <ToggleButton.Style>
                                                <Style TargetType="ToggleButton">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border x:Name="box" Width="20" Height="20" CornerRadius="4" BorderThickness="2" BorderBrush="{DynamicResource BrushTextMuted}" Background="Transparent">
                                                                    <TextBlock x:Name="check" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsChecked" Value="True">
                                                                        <Setter TargetName="box" Property="Background" Value="{DynamicResource BrushAccent}" />
                                                                        <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
                                                                        <Setter TargetName="check" Property="Visibility" Value="Visible" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10 0 0 0">
                                            <TextBlock Text="{Binding DisplayName}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" />
                                            <TextBlock Text="{Binding Id, StringFormat='PID: {0}'}" FontSize="11" Foreground="{DynamicResource BrushTextMuted}" Margin="0 2 0 0" />
                                        </StackPanel>

                                        <Button Grid.Column="2" Width="28" Height="28" Background="Transparent" Foreground="{DynamicResource BrushTextSecondary}" BorderThickness="1" BorderBrush="{DynamicResource BrushBorder}" Cursor="Hand"
                                                Command="{Binding DataContext.TestInstanceCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}" CommandParameter="{Binding Id}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="0">
                                                                    <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
                                                                        <Setter Property="Foreground" Value="{DynamicResource BrushAccent}" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button Grid.Column="4" Style="{StaticResource SmallCloseButton}"
                                                Command="{Binding DataContext.KillProcessCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}" CommandParameter="{Binding Id}" VerticalAlignment="Center">
                                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Background="{DynamicResource BrushSurfaceDarkTertiary}" CornerRadius="10" Padding="20" Visibility="{Binding HasNoProcesses, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="{Binding Loc.NoInstances}" FontSize="13" Foreground="{DynamicResource BrushTextMuted}" HorizontalAlignment="Center" />
                    </Border>

                    <TextBlock Text="{Binding WebhookStatus}" FontSize="12" Foreground="{DynamicResource BrushAccent}" Margin="0 8 0 0" Visibility="{Binding HasWebhookStatus, Converter={StaticResource BoolToVisibility}}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
