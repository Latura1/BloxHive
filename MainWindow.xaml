<Window x:Class="BloxHive.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BloxHive.ViewModels"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        mc:Ignorable="d"
        Title="BloxHive" Width="1200" Height="750"
        WindowStartupLocation="CenterScreen"
        MinWidth="900" MinHeight="600"
        WindowStyle="None"
        Background="{DynamicResource BrushSurfaceDark}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="5"
                            UseAeroCaptionButtons="False"
                            NonClientFrameEdges="None" />
    </shell:WindowChrome.WindowChrome>

    <Border CornerRadius="16" Background="{DynamicResource BrushSurfaceDark}" Margin="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource BrushSurfaceDark}" CornerRadius="16 16 0 0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16 0 0 0" VerticalAlignment="Center">
                        <Border Width="20" Height="20" CornerRadius="6" Background="{DynamicResource BrushAccent}">
                            <TextBlock Text="B" FontSize="11" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="BloxHive" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" Margin="10 0 0 0" VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0 2 0 0">
                        <Button Style="{StaticResource TitleBarButton}" Click="MinimizeClick">
                            <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                        </Button>
                        <Button x:Name="MaximizeButton" Style="{StaticResource TitleBarButton}" Click="MaximizeClick">
                            <TextBlock x:Name="MaximizeIcon" Text="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                        </Button>
                        <Button Style="{StaticResource CloseButton}" Click="CloseClick">
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Body -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{DynamicResource BrushSurfaceDarkSecondary}" CornerRadius="0 0 0 16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0 16 0 0">
                            <TextBlock Text="{Binding Loc.Navigation}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="16 0 0 12" />
                            <Button Style="{StaticResource SidebarButton}" Command="{Binding NavigateToHomeCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE10F;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Width="20" />
                                    <TextBlock Text="{Binding Loc.Home}" Margin="10 0 0 0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SidebarButton}" Command="{Binding NavigateToAccountsCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE716;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Width="20" />
                                    <TextBlock Text="{Binding Loc.Accounts}" Margin="10 0 0 0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SidebarButton}" Command="{Binding NavigateToDashboardCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE7C1;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Width="20" />
                                    <TextBlock Text="{Binding Loc.Dashboard}" Margin="10 0 0 0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SidebarButton}" Command="{Binding NavigateToSettingsCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Width="20" />
                                    <TextBlock Text="{Binding Loc.Settings}" Margin="10 0 0 0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <TextBlock Text="{Binding Loc.Tools}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource BrushTextMuted}" Margin="16 12 0 12" />
                            <Button Style="{StaticResource SidebarButton}" Command="{Binding NavigateToLogsCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE943;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Width="20" />
                                    <TextBlock Text="{Binding Loc.LogsTitle}" Margin="10 0 0 0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SidebarButton}" Command="{Binding NavigateToBackupCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE718;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Width="20" />
                                    <TextBlock Text="{Binding Loc.BackupTitle}" Margin="10 0 0 0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Main Content -->
                <Border Grid.Column="1" Background="{DynamicResource BrushBackground}" CornerRadius="0 0 16 0">
                    <ContentControl Content="{Binding CurrentView}" />
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
